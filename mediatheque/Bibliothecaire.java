package mediatheque;

public class Bibliothecaire extends Acteur {
    // Cette classe hérite de la classe mère Acteur et n'a aucun attribut qui lui
    // est spécifique.
    public Bibliothecaire(String prenom, String nom) { // création du constructeur
	super(prenom, nom);
    }
}
